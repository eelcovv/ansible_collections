- hosts: all
  gather_facts: true
  collections:
    - eelcovv.ldx_webdev_server
  tasks:
    - import_role:
        name: system
    - import_role:
        name: mamba
- hosts: oracledsk
  gather_facts: true
  collections:
    - eelcovv.ldx_webdev_server
  tasks:
    - import_role:
        name: ubuntu-desktop
- hosts: oracleweb
  gather_facts: true
  collections:
    - eelcovv.ldx_webdev_server
  tasks:
    - import_role:
        name: ubuntu-webserver
